How to use a command in command line.

1. cd to folder with emailtask - dobromir matuszak/emailtask

2.Available command to rise by terminal

    1. -h                           # help
    2. -ir                          # show incorrect email
    3. -s (argument)                # after -s add argument to search by him in all email
    4. -gbd                         # group by domain
    5. -feil (argument)             # after -feil add argument which is path, email-sent.logs